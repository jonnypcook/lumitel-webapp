import { Component } from '@angular/core';

@Component({
    selector: 'loader',
    template: `<small>Please wait whilst page is loaded ...</small>`
})
export class LoaderComponent {
    constructor() {}
}
